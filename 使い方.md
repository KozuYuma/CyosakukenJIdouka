# 著作権調査支援ツール　使い方

---

## 起動する

`run.bat` をダブルクリックする。

- 初回だけ自動でセットアップが走る（2〜3分）
- ブラウザで `http://localhost:8501` が開いたら準備完了
- 開かない場合は手動でブラウザに `http://localhost:8501` と入力する

終了するには、起動中の黒いウィンドウで `Ctrl + C` を押す。

---

## 手順

### ① WAV 一覧 CSV を作る

PowerShell を開いて以下を実行する。

```powershell
H:\PROGRAM\CyosakukenJIdouka_app\scripts\Get-WavList.ps1 `
  -AudioFolder "H:\プロジェクト名\Audio" `
  -OutputCsv "H:\Desktop\wav_list.csv"
```

`wav_list.csv` がデスクトップに作られる。

MP3 一覧も必要な場合（WAV で見つからなかった曲の補助）：

```powershell
H:\PROGRAM\CyosakukenJIdouka_app\scripts\Get-Mp3List.ps1 `
  -Mp3Folder "H:\MP3ライブラリ" `
  -OutputCsv "H:\Desktop\mp3_list.csv"
```

---

### ② NUENDO から Cue CSV を書き出す

NUENDO のメニューから CSV を書き出す。  
書き出し方法はバージョンによって異なる。

**例（Nuendo 12 以降）**  
ファイル → 書き出し → トラックアーカイブ... ではなく、  
プール → 選択して書き出し などから CSV を選ぶ。

> バージョンが分からない場合は、NUENDOで普段使っている書き出し方法でOK。  
> カンマ区切り・タブ区切り・セミコロン区切りのどれでも自動で読み込める。

---

### ③ CSV を読み込む

「📁 ファイル読み込み」タブを開く。

| 項目 | 内容 |
|------|------|
| ① NUENDO Cue CSV | NUENDO から書き出した CSV |
| ② WAV 一覧 CSV | ①で作った wav_list.csv |
| ③ MP3 一覧 CSV | 任意。WAV で見つからない曲がある場合のみ |
| ④ 既存調査 Excel | 前回の続きから始める場合（将来対応予定） |

アップロード後に件数が表示されたら **「照合実行」** ボタンを押す。

---

### ④ 楽曲まとめを確認する

「🎵 楽曲まとめ」タブを開く。

**WAV照合ステータスの見方**

| ステータス | 意味 |
|-----------|------|
| 完全一致 | ファイル名が完全に一致 |
| 正規化一致 | 拡張子・記号を除いて一致 |
| 管理番号一致 | 管理番号（6ST-653-09 など）が一致 |
| 曲名一致 | 管理番号を除いた曲名部分が一致 |
| ファイル名一致 | NUENDO 上のファイル名で一致 |
| 部分一致 | 名前の一部が含まれる |
| WAV未照合 | 自動では見つからなかった |

**WAV未照合の曲が多い場合**  
→ イベント名とWAVファイル名の付け方が大きく違う可能性がある。  
→ 「メモ」欄に手掛かりを書いておいて、後で手動で対応する。

**編集できる列**（セルをクリックして直接入力）

- 作曲者 / 作詞者 / アーティスト
- CD番号
- JASRAC作品コード
- NexTone管理番号
- 確認ステータス
- メモ

---

### ⑤ J-WID / NexTone で自動調査する

「🔍 検索補助」タブを開く。

1. **楽曲をプルダウンで選ぶ**
2. **検索語を選ぶ**（WAV検出タイトル・管理番号・盤番号などが自動で候補に出る）
3. **「自動検索」ボタン**を押す（J-WIDとNexToneを順番に検索する。約4秒かかる）
4. **結果を確認する**
   - 「J-WID 結果」タブと「NexTone 結果」タブに分かれて表示される
   - 複数候補が出た場合は内容を見て正しいものを選ぶ
5. **「✅ このデータを楽曲まとめに適用」** を押す
   - 作曲者・作詞者・JASRAC作品コード / NexTone管理番号が自動で入力される
   - 確認ステータスが「候補あり」に変わる

> **検索してもヒットしない場合**  
> - 検索語を変えてみる（曲名の表記がゆれている場合がある）  
> - 「🔍 J-WID トップ」ボタンでサイトを開いて手動検索する  
> - 「🔍 Google で検索」で手掛かりを探す

> **「エラー」や「HTML パース失敗」と出た場合**  
> サイトの仕様が変わってパースに失敗している可能性がある。  
> 「デバッグ: 取得 HTML」を開いてHTMLの内容をコピーして開発者に共有する。

---

### ⑥ 確認ステータスを更新する

「🎵 楽曲まとめ」タブで「確認ステータス」列をクリックするとプルダウンが出る。

| ステータス | 使うタイミング |
|-----------|-------------|
| 未調査 | まだ何もしていない（初期値） |
| 候補あり | 自動調査で候補が出たが最終確認前 |
| 確定 | 権利情報を確認済み |
| 要確認 | 何か問題がある |
| J-WID要確認 | J-WIDで見つかったが内容の確認が必要 |
| NexTone要確認 | NexToneで見つかったが内容の確認が必要 |
| ライブラリ元確認 | ライブラリ会社への問い合わせが必要 |
| Audiostock確認 | Audiostockでの確認が必要 |
| MP3補助確認 | WAVで照合できなかった。MP3で補助確認が必要 |

---

### ⑦ Excel に出力する

「📊 Excel 出力」タブを開く。

1. ファイル名を入力（デフォルト: `著作権調査.xlsx`）
2. **「Excel ファイルを生成」** ボタンを押す
3. **「ダウンロード」** ボタンを押して保存する

**出力されるシート**

| シート名 | 内容 |
|---------|------|
| 楽曲まとめ | 権利調査用メインシート |
| イベント一覧 | NUENDO イベント単位の全一覧 |
| WAV 一覧 | 読み込んだ WAV ファイル一覧 |
| MP3 一覧 | 読み込んだ MP3 ファイル一覧 |
| 検索語一覧 | J-WID / NexTone 検索用語一覧 |
| 確認メモ | 処理ルール・ステータス凡例 |

---

## よくあるトラブル

**Q. 「読み込みエラー」が出る**  
A. CSV を Excel で開いて「名前を付けて保存」→「CSV UTF-8（コンマ区切り）」で保存し直してから再アップロードする。

**Q. ほとんどの曲が「WAV未照合」になる**  
A. イベント名とWAVファイルの命名ルールが合っていない。  
　 WAVファイル名とイベント名の対応を確認して、メモ欄に記録しておく。

**Q. J-WID / NexTone の検索結果が出ない**  
A. サイトの仕様変更の可能性がある。手動で検索リンクを使う。

**Q. アプリを閉じたら入力データが消えた**  
A. ブラウザを閉じるとデータが消える。作業途中でこまめに Excel 出力しておく。  
　 次回は出力した Excel を読み込み直す機能を今後追加予定。
